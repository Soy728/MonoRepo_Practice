// parser를 가져다 써보자.
// import { parse } from "parser";

// const testParser = parse(30);
// console.log(testParser);

const { parse } = require("parser");

const testParser = parse(30);
console.log(testParser);
